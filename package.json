{
  "name": "bomb",
  "version": "1.0.0",
  "description": "app which waits for a code to disable the bomb",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
